databases:
  - name: ekiptahmin-db
    plan: basic-256mb
    databaseName: ekiptahmin
    user: ekiptahmin
    region: frankfurt

services:
  - type: web
    name: ekiptahmin
    runtime: python
    plan: starter
    region: frankfurt
    buildCommand: "./build.sh"
    startCommand: "gunicorn config.wsgi:application --workers 2 --threads 2"
    healthCheckPath: /healthz/
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.10"
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.prod
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ekiptahmin.com,www.ekiptahmin.com,.onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: ekiptahmin-db
          property: connectionString
      - key: SITE_URL
        value: https://ekiptahmin.com
      - key: DEFAULT_FROM_EMAIL
        value: noreply@ekiptahmin.com
      - key: RESEND_API_KEY
        sync: false
      - key: SENTRY_DSN
        sync: false
      # Svix signing secret for the Resend bounce/complaint webhook (Faz 1.3).
      # Only the web service serves the endpoint, so the crons don't need it.
      - key: RESEND_WEBHOOK_SECRET
        sync: false

  # Daily digest cron jobs. Times are UTC; the app runs in Europe/Istanbul
  # (UTC+3, no DST), so 10:00 UTC = 13:00 TRT and 05:00–09:00 UTC = 08:00–12:00 TRT.
  # NOTE: RESEND_API_KEY is sync:false — set it on EACH cron service in the
  # Render dashboard (separate from the web service), or no mail is delivered.
  #
  # Build is intentionally lean (pip only): the crons don't serve static/CSS and
  # must NOT run migrate/seed/recompute — the web service owns those, and running
  # them concurrently from three services against the one DB races at deploy time.
  - type: cron
    name: ekiptahmin-digest-morning
    runtime: python
    region: frankfurt
    schedule: "0 10 * * *"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python manage.py send_daily_digest --mode morning"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.10"
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.prod
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ekiptahmin.com,www.ekiptahmin.com,.onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: ekiptahmin-db
          property: connectionString
      - key: SITE_URL
        value: https://ekiptahmin.com
      - key: DEFAULT_FROM_EMAIL
        value: noreply@ekiptahmin.com
      - key: RESEND_API_KEY
        sync: false
      - key: SENTRY_DSN
        sync: false

  - type: cron
    name: ekiptahmin-digest-evening
    runtime: python
    region: frankfurt
    schedule: "0 5-9 * * *"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python manage.py send_daily_digest --mode evening"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.10"
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.prod
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ekiptahmin.com,www.ekiptahmin.com,.onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: ekiptahmin-db
          property: connectionString
      - key: SITE_URL
        value: https://ekiptahmin.com
      - key: DEFAULT_FROM_EMAIL
        value: noreply@ekiptahmin.com
      - key: RESEND_API_KEY
        sync: false
      - key: SENTRY_DSN
        sync: false

  # Live-result sync. The homepage trigger (maybe_sync_live) only fires while a
  # visitor is on the site, so a match that finishes with nobody watching — or a
  # match in the dead-of-night window — never gets its final score polled, and a
  # missed FINISHED leaves MatchSync stuck IN_PLAY forever. This cron is the
  # guaranteed server-side poll: sync_live_results captures scores (cheap no-op
  # when nothing is in the live window) and finalize_stale_syncs flips matches
  # that are over but missed their FINISHED signal. Both are idempotent and run
  # safely alongside the visitor trigger.
  # NOTE: FOOTBALL_DATA_API_KEY is sync:false — set it on THIS cron service in
  # the Render dashboard (separate from the web service), or it can't reach the
  # provider and silently no-ops.
  - type: cron
    name: ekiptahmin-live-sync
    runtime: python
    region: frankfurt
    schedule: "*/5 * * * *"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python manage.py sync_live_results && python manage.py finalize_stale_syncs"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.10"
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.prod
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ekiptahmin.com,www.ekiptahmin.com,.onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: ekiptahmin-db
          property: connectionString
      - key: SITE_URL
        value: https://ekiptahmin.com
      - key: DEFAULT_FROM_EMAIL
        value: noreply@ekiptahmin.com
      - key: FOOTBALL_DATA_API_KEY
        sync: false
      - key: SENTRY_DSN
        sync: false
