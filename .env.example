# Django
DJANGO_SETTINGS_MODULE=config.settings.dev
SECRET_KEY=change-me-to-a-random-50-char-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (local PostgreSQL)
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgres://postgres:YOUR_PG_PASSWORD@localhost:5432/ekiptahmin_dev

# Production database (read-only source for scripts/pull-prod-db.ps1)
# Render Dashboard -> ekiptahmin-db -> Connect -> External Database URL
# Append ?sslmode=require if the URL does not already include it.
PROD_DATABASE_URL=

# Email (Resend) - leave blank for dev (uses console backend)
RESEND_API_KEY=
DEFAULT_FROM_EMAIL=noreply@ekiptahmin.com

# Live results (football-data.org) - leave blank to disable the live sync.
# Get a key at https://www.football-data.org/client/register
# Safe to set locally to test finished matches: `python manage.py fd_probe --finished`
FOOTBALL_DATA_API_KEY=

# Site
SITE_URL=http://localhost:8000
